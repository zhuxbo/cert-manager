# Manager 路由入口 —— 由 BT 站点 vhost 的 server 块 include。
# 本文件受管,升级覆盖。自定义请走 nginx/custom/(升级不覆盖),由 render.sh 渲染到 enabled/。
include __PROJECT_ROOT__/nginx/enabled/routes/*.conf;

# 插件 Nginx 配置(插件自管,内联保留)
include __PROJECT_ROOT__/plugins/*/nginx/*.conf;
