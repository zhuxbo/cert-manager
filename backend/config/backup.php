<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 备份加密密钥
    |--------------------------------------------------------------------------
    |
    | 安全基线：备份加密默认启用。
    | install.sh 自动写入 .env 中 BACKUP_ENC_KEY=$(openssl rand -hex 32)。
    | 32 字节 = 64 个十六进制字符 = AES-256 密钥。
    |
    | 丢失=备份不可恢复，请离线保存。
    |
    */
    'enc_key' => env('BACKUP_ENC_KEY'),
];
